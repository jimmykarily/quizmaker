apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - cookie-secret.yaml

configMapGenerator:
  - name: quizmaker-questions
    files:
      - questions.yaml

generatorOptions:
  disableNameSuffixHash: true
