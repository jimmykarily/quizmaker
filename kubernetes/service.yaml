apiVersion: v1
kind: Service
metadata:
  name: quizmaker-service
spec:
  selector:
    app: quizmaker
  ports:
    - port: 80           # Port for inside-cluster access
      targetPort: 8080   # Port the container listens on
